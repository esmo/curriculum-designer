# Collaboration Rules

- Before implementing changes, first answer the user's questions and provide a short proposal.
- Only start implementation after explicit user approval (for example: "ja", "bitte umsetzen").
- If requirements are ambiguous, ask a clarifying question instead of coding immediately.
- Do not provide unsolicited implementation suggestions. Only propose next steps when the user explicitly asks for suggestions.
